#!/bin/sh
# $3 is 1 for a branch checkout, 0 for a file checkout. Only branch switches can
# change which skills exist, so ignore the rest.
[ "$3" = "1" ] || exit 0
root=$(git rev-parse --show-toplevel)
[ -x "$root/bin/skills-sync" ] || exit 0
"$root/bin/skills-sync" refresh --quiet || true
exit 0
