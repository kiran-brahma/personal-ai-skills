# Codex Security adapter

Codex Security remains an external, separately loaded package. Use it only when the user requests a detailed security audit, threat model, vulnerability scan, finding triage, or security hardening review.

Official documentation and workflows: [Codex Security](https://developers.openai.com/codex/security).

Do not copy its package into this public skill library. The upstream package is proprietary, includes connectors and a bundled MCP server, and has operational permissions beyond an instruction-only skill. Review and install it through the official distribution path when needed.
