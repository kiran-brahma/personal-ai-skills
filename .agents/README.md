# Agent discovery layer

This directory is an integration point for agents that discover skills through `.agents`.

The canonical skill source remains `../skills/`. Do not author or maintain a second copy here. The future installation/setup process may place links or generated discovery entries here so agents can use the committed canonical skills.

Claude-specific setup is documented separately under `../adapters/claude/`.
